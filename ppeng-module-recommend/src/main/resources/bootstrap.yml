spring:
  cloud:
    nacos:
      # 配置中心
      config:
        server-addr: ****:8848
        enabled: true
        file-extension: yaml
        group: PPENG_GROUP
        namespace: ppeng
        username: ppeng
        password: **confidential**
        shared-configs[0]:
          data-id: redis.yaml
          group: PPENG_GROUP
          refresh: true
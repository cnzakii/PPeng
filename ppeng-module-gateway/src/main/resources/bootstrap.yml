spring:
  cloud:
    nacos:
      # 配置中心
      config:
        server-addr: ****:8848
        enabled: true
        file-extension: yaml
        group: PPENG_GROUP
        namespace: ppeng
        username: ppeng
        password: **confidential**
        shared-configs[0]:
          data-id: logback.yaml # 配置文件名-Data Id
          group: PPENG_GROUP   # 默认为DEFAULT_GROUP
          refresh: true   # 是否动态刷新，默认为false
        shared-configs[1]:
          data-id: redis.yaml
          group: PPENG_GROUP
          refresh: true
        shared-configs[2]:
          data-id: sa-token.yaml
          group: PPENG_GROUP
          refresh: true








